# Spring Properties
spring:
  datasource:     # Datasource Properties
    url: jdbc:postgresql://postgresql-db:5432/review_system_db
    username: user
    password: user123
  config:         # Config Properties
    import: optional:configserver:http://config-server:8085