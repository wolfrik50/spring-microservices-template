# Application and Datasource Properties
spring:
  application:
    name: hotel-service
  datasource:
    url: jdbc:postgresql://127.0.0.1:5432/review_system_db
    username: wulf
    password: wulf123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true

server:
  servlet:
    context-path: /api
  port: 8082

logging:
  level:
    root: DEBUG
